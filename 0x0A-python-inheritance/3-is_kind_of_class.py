#!/usr/bin/python3
"""here you can find the function is_kind_of_class"""


def is_kind_of_class(obj, a_class):
    """function that returns true or false
        if the object is an instance of the specified class"""
    return (isinstance(obj, a_class))
